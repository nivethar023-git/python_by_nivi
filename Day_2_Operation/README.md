Concept:
Uses conditional checks to perform operations like addition, subtraction, multiplication, and division.

What it does:
Accepts two numbers and an operator, then returns the calculated result.
